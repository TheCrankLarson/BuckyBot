// FindRace function
function FindRace(raceName)
{
    // We are looking for an event called (or containing) raceName, and will send results to informChannel

    // 
    return raceName + ' not found.';
}